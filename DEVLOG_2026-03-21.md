# 開発ログ — 2026-03-21

## 今日やったこと

### 1. 特商法ページの記載
- 販売業者: 林 治哉
- 所在地: 請求があり次第、遅滞なく開示
- メール: haruya.h@icloud.com

---

### 2. FEATURESセクションの更新
- 「1週間まとめて生成」→「⭐ 作った料理を保存・評価」に変更（機能廃止に合わせた訴求変更）

---

### 3. 競合リサーチ → TODO.md に記載
- me:new、DELISH KITCHEN、おいしい健康、Frish/pecco など10アプリを調査
- 差別化ポイント: 料理評価機能・包丁不要フィルター・記録機能が競合未実装

---

### 4. Clerk ログイン機能の実装
- `@clerk/nextjs` を導入（日本語ローカライズあり）
- サブスク済みユーザーのみログイン＆Redis保存が可能
- 管理者はサブスクなしでもログインできる（deviceId で判定）
- standard/premium 未ログイン時に生成ボタン押下でログイン促進モーダル表示
- `/api/user-data` エンドポイント新設（Clerk userId キーで Redis に cooked / prefs を保存）

---

### 5. ヘッダーのナビデザイン刷新
- アイコン上・ラベル下のデリッシュキッチン風レイアウトに変更
- 生成中・生成後でもレイアウト崩れなし

---

### 6. 初回オンボーディングモーダル実装
- Step1: 家族人数（グリッド選択）
- Step2: アレルギー（14種チップ選択）
- 完了後に familySize・disliked へ反映、localStorage に `meshilist_onboarded` フラグ保存

---

### 7. 料理名を食欲をそそる表現に変更
- AIプロンプトに指示追加
- 例: 「とろーり濃厚♪ エビのチーズクリームリゾット」

---

### 8. レシピ表示のリデザイン
- 旧: テキストのみ
- 新: 写真(240px) → 栄養情報（横並びボックス）→ 材料（1列）→ 作り方（写真なし）→ コツ → 評価

---

### 9. SNSシェアのテキスト分岐
- 管理者: サービス訴求文＋URL＋ハッシュタグ（拡散用）
- サブスクユーザー: 料理名＋ハッシュタグのみ（シンプル）

---

### 10. 材料を2列 → 1列表示に変更
- 2列だと読みにくいという指摘を受けて修正

---

### 11. 管理者の自動保存＋履歴カードUIの実装
- 管理者デバイスで生成するとレシピが全て自動で記録に保存（評価🤩固定）
- `CookedRecord` に `imageUrl` フィールドを追加
- `imageResultsRef` を導入して非同期完了後も画像URLを参照可能に
- 記録モーダルを2列カードUIに刷新（上半分:写真、下半分:料理名＋材料タグ）

---

### 12. 調理時間チップの廃止＋生成結果に調理時間を追加
- 入力フォームの「調理時間の目安」チップを削除
- AIプロンプト出力形式に `⏱ 調理時間: XX分` を追加
- RecipeBlock に水色バッジで表示（栄養情報の直下）

---

### 13. 管理者判定をサーバーサイドに統一（重要修正）
- `NEXT_PUBLIC_ADMIN_DEVICE_ID` によるクライアント比較を廃止
- `/api/trial` のレスポンスに `isAdmin: boolean` を追加（サーバーの `ADMIN_DEVICE_ID` で判定）
- `canSave` / 生成ゲート / 画像生成トリガー / ログインボタン表示を全て `trialStatus.isAdmin` で統一

---

### 14. 管理者の生成ブロック解除
- 旧: `!trialActive && !subscribed` で Stripe へ飛ばされていた
- 修正: `isAdminDevice` の場合は全てのゲートをバイパス
- 画像生成トリガーの `if (trialStatus?.trialActive)` にも管理者バイパスを追加
- 自動保存のループを一括 `setCookedRecords` に変更（累積バグ修正）

---

### 15. 管理者の記録カードタップでレシピ詳細表示
- 管理者限定: 記録カードをタップ → RecipeBlock を使った詳細シートが下からスライドアップ
- 写真・栄養情報・調理時間・材料・手順・コツを全表示
- ×ボタン or 背景タップで閉じる

---

### 16. ヘッダーから「レシピ集」を削除
- 未実装のため一時削除

---

### 17. 画像が断続的に生成されないバグを修正（重要）

**原因:**
- `output_format: "jpeg"` がコメントにだけ書かれていて、実際の API コールに渡されていなかった
- PNG b64 レスポンスが 3〜5MB → Vercel の 4.5MB レスポンス上限を超えてランダムに失敗
- MIME タイプも誤り（JPEG なのに `data:image/png;base64,` にしていた）

**修正内容:**
- `output_format: "jpeg"` + `output_compression: 75` を実際に渡す（PNG 比 1/5 に削減）
- `quality: "low"` に統一（最速・最小）
- 失敗時に 1 秒待って自動リトライ（最大2回）
- `data:image/jpeg;base64,` として正しい MIME タイプで返す
- クライアントも `dataUrl` フィールドを受け取るよう修正

---

## 残課題・メモ

- 記録がまだ溜まっていない（今後生成するたびに自動保存される）
- 画像が時間経過で消える件 → OpenAI の一時 URL は1時間で失効するが、現在は base64 で保存しているため解消済み
- localStorage に base64 画像を大量に保存すると容量上限（5〜10MB）に当たる可能性あり → 50件スライスで対処中
